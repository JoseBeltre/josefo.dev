---
interface Props {
  class?: string
}

const { class: className } = Astro.props
---

<span
  class={`bg-accent/20 text-accent p-1.5 rounded-full px-4 text-sm font-medium border border-accent ${className}`}
>
  {'{'}
  <slot />
  {'}'}
</span>
