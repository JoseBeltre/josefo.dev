---
interface Props {
  class?: string
}

const { class: className } = Astro.props
---

<span
  class={`bg-primary p-1.5 rounded-full px-4 text-sm font-medium lg:text-xs ${className}`}
>
  {'{'}
  <slot />
  {'}'}
</span>
